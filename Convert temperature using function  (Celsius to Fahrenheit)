#include <stdio.h>

float toFahrenheit(float c) {
    return (c * 9 / 5) + 32;
}

int main() {
    float c;
    printf("Enter Celsius: ");
    scanf("%f", &c);

    printf("Fahrenheit = %.2f\n", toFahrenheit(c));
    return 0;
}
