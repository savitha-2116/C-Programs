#include <stdio.h>

float calculateBill(float price, int qty) {
    return price * qty;
}

int main() {
    float price;
    int qty;

    printf("Enter price: ");
    scanf("%f", &price);
    printf("Enter quantity: ");
    scanf("%d", &qty);

    printf("Total Bill = %.2f\n", calculateBill(price, qty));
    return 0;
}
