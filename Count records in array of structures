#include <stdio.h>

struct Student {
    int id;
    char name[20];
};

int main() {
    int n;
    struct Student s[10];

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Total records stored = %d\n", n);
    return 0;
}
